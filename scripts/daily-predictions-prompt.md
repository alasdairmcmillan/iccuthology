SELF-CHECK FIRST: this prompt is the full contents of `scripts/daily-predictions-prompt.md` and ends with a STEP 5 titled "report". If what you received ends anywhere before that (e.g. mid-sentence), the delivery truncated it — Read `scripts/daily-predictions-prompt.md` from the repo and follow the file, not the truncated copy. You are running headlessly: never stop to ask a question, there is no one to answer it.

You are the daily orchestrator for the iccuthology Phish setlist predictor repo. You are running locally in a headless `claude -p` session against the real repo checkout at `D:\dev\iccuthology` — the real venv, `data/phish.db`, and `.env`/`.env.local` credentials are all present.

Read AGENTS.md and docs/MCP.md first; they are the authoritative playbook for identifying yourself, researching a show, and submitting predictions (model_label rules, the 20-40 song shortlist + calibration guidance, setlist scoring, the submit_prediction call shape). This prompt only adds the daily orchestration steps around that playbook.

SPECIAL CONTEXT — 2026 MSG anniversary run (7/22-7/29, venueid 157): this is a 5-night run at Madison Square Garden that happens to land on Phish's 92nd through 96th shows at that venue, and the band is leaning hard into that numerology — each night's setlist skews toward material from the single calendar year matching its own show number (92nd show -> 1992, 93rd -> 1993, 94th -> 1994, 95th -> 1995, 96th -> 1996: debuts that year, songs in heaviest rotation that year, and a materially higher-than-normal bustout rate for deep-gap songs tied to that year), confirmed against the actual 7/24 (93rd show) setlist — e.g. Daniel Saw the Stone debuted '93, Sparks/Slave/Makisupa were massive gap bustouts that resurfaced in '93, Weigh had its single busiest year in '93. If any of 07-25/07-27/07-29 (94th/95th/96th) are still upcoming when this prompt runs, treat this as the dominant signal for those shows specifically: pull each night's shortlist and setlist call from that night's matching year (song_history/candidate_features with a wide `top` — the relevant songs are often 90s deep cuts with low decayed_rate, so they fall outside the default top-50 cutoff) rather than generic current-rotation heuristics, and note the year-mapping explicitly in the rationale. This is a one-off pattern for this specific run, not a standing rule — do not apply it to unrelated multi-night runs unless the same anniversary-numerology signal shows up again (Nth show at a venue lining up with a plausible year).

STEP 1 — check whether the THREE PREDICTION TRACKS are stale. There are TWO independent ways they can be stale and you must check BOTH: (i) a show was PLAYED that the tracks haven't predicted against since, and (ii) a show was ANNOUNCED that one or more tracks have never predicted at all. Checking only (i) is the bug that silently idled this routine for sixteen days in August 2026 — see 1.5. This is also NOT the same question as "did the DB just ingest something": the DB refresh below always runs and is usually a no-op on its own, and an ingest that adds only future shows moves no show_index. The three live tracks are claude-sonnet/claude-fable/claude-opus. claude-haiku was ELIMINATED on 2026-08-18 (`phishpred.tracks.ELIMINATED` is the source of truth) -- do NOT generate, dispatch, or expect predictions for it. Its scored record stays on the public scoreboard marked eliminated; that is deliberate, not a gap to backfill. Do NOT use any file under `agents/<other-harness>/` (e.g. `agents/antigravity/last_seen_showdate.txt`) as your freshness signal — that belongs to a different, unrelated harness's own tracking and has caused a false "nothing to do" conclusion before.

1. From the repo root run: `python -m uv run python -m phishpred.cli refresh` (or `.\.venv\Scripts\python.exe -m phishpred.cli refresh` if uv isn't on PATH) — idempotent, always safe to run, brings `data/phish.db` current with phish.net.
2. Compute the current max played show index: `python -c "from phishpred.db import get_connection; c = get_connection('data/phish.db'); print(c.execute('SELECT MAX(show_index) FROM shows WHERE show_index IS NOT NULL').fetchone()[0])"`.
3. Read this routine's own bookmark file, `tmp/prediction-orchestrator-state.txt` (plain integer = the max show_index the live tracks last generated predictions against). If the file doesn't exist, treat it as unset — i.e. always proceed (first run).
4. Compute the COVERAGE GAP — upcoming shows that have no prediction yet. A newly ANNOUNCED show is new work even though no show has been played, and this is the trigger the bookmark alone cannot see (a whole new tour can appear without max show_index moving at all). Run:

```
python -c "from phishpred.db import get_connection; from phishpred.mcp import tools; import os, datetime; c = get_connection('data/phish.db'); today = datetime.date.today().isoformat(); tracks = ['claude-sonnet','claude-fable','claude-opus']; shows = [s['showdate'] for s in tools.upcoming_shows(c, limit=50)['shows'] if s['showdate'] > today]; gap = [d for d in shows if not all(os.path.exists(f'data/predictions/submitted/{t}/{d}.json') for t in tracks)]; print('upcoming:', len(shows)); print('COVERAGE GAP:', gap)"
```

5. Decide using BOTH signals. Proceed to STEP 2 if EITHER is true:
   - the current max show_index is > the bookmarked value (a new show was PLAYED — restate every upcoming show, since new setlist data changes the calls for all of them); OR
   - the coverage gap from 1.4 is non-empty (shows were ANNOUNCED that one or more of the three live tracks have never predicted).

   Only if BOTH are false is there nothing to do: STOP HERE — do not dispatch any subagents, do not touch data/predictions/ or R2. Report "no new shows since last run" including the current max show_index, the bookmark value, AND the (empty) coverage gap so it's auditable, and end.

   REAL FAILURE this guards against: from 2026-08-03 through 2026-08-18 this routine no-opped for sixteen consecutive days. The task was healthy and exited 0 every morning, and the health markers stayed OK, because the gate only asked whether a show had been PLAYED. The last played show was 2026-08-01 (show_index 1961, bookmark 1961), while the 8-show 2026 Fall Tour (tourid 223, Oct 2-11) had been announced and ingested with zero predictions from any track. A correct run must wake up for that.

6. Whichever signal fired, note the current max show_index; you'll write it to the bookmark file at the very end (step 4 below), but ONLY if the R2 push in step 4 actually succeeds. If ONLY the coverage gap fired and no new show was played, the bookmark value will be unchanged — write it anyway (harmless) and rely on the coverage gap to keep driving future announcement-only runs.
7. SCOPE when only the coverage gap fired: target the shows in the gap. You may skip upcoming shows that all three live tracks already cover, unless a new show was also played (in which case restate everything).

STEP 2 — identify every future/upcoming show still on the schedule (AGENTS.md "Calculate the target shows" has the one-liner query). Generate YOUR OWN predictions (you are Sonnet) for every target show, following AGENTS.md steps 1-5 exactly, using model_label `claude-sonnet` (this label already exists under `data/predictions/submitted/claude-sonnet/` — reuse it exactly, do not invent a variant). Use the direct-python fallback (`phishpred.mcp.tools` driven via the venv python) since no live MCP server is attached in this session — it's the identical code path. Submit ALL THREE parts per show (predictions, rationale, setlist) per AGENTS.md step 4.

Then dispatch two subagents IN PARALLEL (one message, two Agent tool calls, each with `run_in_background: false`) to do the same research-and-submit workflow, one per remaining live track, reusing these exact existing labels:
- model `fable`, model_label `claude-fable`
- model `opus`, model_label `claude-opus`

Do NOT dispatch a claude-haiku subagent, and never resurrect an eliminated label to close a coverage gap.

IMPORTANT: this whole prompt runs headlessly via `claude -p` (one-shot, non-interactive) — there is no follow-up turn for a background task's completion notification to land in. The Agent tool defaults to backgrounding a dispatched subagent and notifying the *next* turn when it finishes, which only works in an interactive session. In this headless invocation, `run_in_background: false` is required so the three Agent calls actually block until each subagent finishes, before this turn continues to STEP 3. Without it, this turn will emit its dispatch message, be treated as complete, and exit — orphaning the subagents mid-research and silently skipping verification and the R2 push entirely (this happened for real on 2026-07-18: sonnet, generated synchronously before dispatch, got all 13 shows; the three backgrounded subagents got cut off at 11, 5, and 3 shows respectively when the process exited early).

Each subagent prompt must be self-contained: tell it to read AGENTS.md and docs/MCP.md itself, give it its EXACT model_label to reuse (it must not choose its own label), give it the list of target showdates from this run GROUPED BY RUN (which dates share a venue/run), tell it to use the direct-python fallback (no live MCP server), and tell it NOT to touch data/phish.db (read-only) and NOT to run r2_push itself — that happens after your verification pass in step 3. If any target showdate falls within the MSG anniversary run described above, copy the full "SPECIAL CONTEXT" paragraph into that subagent's prompt verbatim — it will not otherwise see the rest of this file, and generic current-rotation reasoning will miss the theme entirely.

For any run 3+ nights long, explicitly warn the subagent in its prompt: a real claude-haiku run on 2026-07-16 drafted one setlist "for the run" and copy-pasted it across nights — this passed fine on 2-night runs but broke down on a 5-night MSG run (nights 2-5 came out as near-duplicates of night 1) and partially on a 3-night run (night 1 and night 3 were identical). Tell it to process each night of a long run as its own research pass and keep a running exclusion set of every slug already committed to earlier nights of that same run, not just the immediately preceding night.

STEP 3 — verify before publishing. Do not skip this; it caught a real bug before (a subagent once submitted verbatim heuristic-baseline probabilities under its own label instead of reasoning). For every track that just submitted today (your own claude-sonnet plus the two subagents), inspect each `data/predictions/submitted/<label>/<showdate>.json` it touched and check: (a) model_label is exactly the expected label for that track; (b) probabilities are not a near-verbatim copy of `tools.heuristic_prediction(conn, showdate)` for that show — if most probs match to 4 decimal places, that track plagiarized the baseline instead of reasoning, and should be rejected; (c) `setlist.sets` is present and non-empty; (d) rationale text differs across that track's showdates, not copy-pasted; (e) no repeated slug within one show's setlist; (f) MECHANICAL setlist-overlap check across ALL PAIRS in the track's batch, not just pairs inside the same run. For every pair of showdates that track submitted today, compute the set overlap between their setlist song lists. Two thresholds:
    - WITHIN a same-venue run: more than ~2-3 shared slugs is a failure — that track copy-pasted a setlist across nights instead of researching each one (real failure: claude-haiku on 2026-07-16 submitted a 5-night MSG run where several night-pairs shared 16-17 of 17 songs).
    - ACROSS different runs: moderate overlap is CORRECT and expected — the same rotation core recurs at different venues, and measured cross-tour repeat lift rises from ~0.02 at adjacent shows to ~1.0 by six shows apart, so do NOT penalize a track for 5-10 shared slugs between runs a week apart. But more than ~13 of ~18 songs (~75%) between ANY two shows is near-duplication and fails.
    Check every run independently — a track that passed on a 2-night run in the same batch is not proof it handled a 4-5 night run correctly.
    WHY ALL PAIRS: on 2026-08-18 claude-haiku submitted 8 fall-tour shows where 2026-10-02, 10-06 and 10-09 had byte-identical slug lists, identical probability vectors and 18-of-18 identical setlists — three different venues in three states. Within-run overlap was 0 for every run, so the old per-run-only version of this check passed it clean. The duplication was entirely across runs.
(g) FORMULA-SIGNATURE check — catch rank-bucket generation that (b) misses, since a track can differ completely from the heuristic baseline and still be mechanical. IMPORTANT: only the first item below is a hard reject. The rest are SIGNALS that should make you look harder at whether the reasoning is real — they are not grounds for rejection on their own, and a track that clears them is not thereby proven good.
    - HARD REJECT: two shows in the batch share an identical probability vector, an identical slug list, or an identical setlist. This is definitional, not a heuristic — one artifact reused across two different shows is not per-show reasoning, whatever the rationales claim.
    - SIGNAL, judge in context: any single probability value repeated across more than ~a third of a show's list (claude-haiku's 2026-10-04 had one value 23 times = 66% of 35 songs, and a retry gave all 35 songs the SAME probability — that end of the scale is damning). A deliberately flat tier of genuinely comparable mid-rotation songs is legitimate.
    - SIGNAL, judge in context: probability sums pinned to the same value across the whole batch. An honest batch varies the sum by per-show confidence (a good example landed 5.72-6.98 across 8 shows), so identical sums mean the track normalized to a target and discarded its own confidence signal. That is a calibration criticism worth reporting, NOT a reject — claude-sonnet pinned every show to 6.80 on 2026-08-18 and its underlying per-show work was genuine.
    Do not let these signals substitute for the real question, which is (i) is this per-show reasoning, and (ii) is it trying to beat the heuristic. Judged mechanically, gemini-flash would have been condemned for a repeated probability ladder while actually scoring level with the heuristic and 2nd of six on weighted score.
(h) SUM-FLOOR check — probabilities are never scaled UP at publish, so a show whose probs sum far below the ~6-9 band forfeits most of its available score before it is even scored. Flag any show summing below ~5, and treat below ~4 as a serious quality problem to report (claude-haiku submitted 2.81 and 3.88 on 2026-08-18). Prefer sending the track back to recalibrate over rejecting outright: a low sum is a self-inflicted scoring loss rather than dishonest output, and a genuinely low-confidence show is allowed to sum lower than a confident one.

If a track fails any check for a given show, exclude just that show's file from the R2 push (not necessarily the whole track — other shows from the same track that pass are still pushed) — note which track/showdate failed and why, for a human to look at later.

STEP 4 — publish. From the repo root, push whatever passed verification to R2's `submitted/` prefix (AGENTS.md step 6). `r2_push.py` accepts either a directory (recursive) or a single file:
- If a track's ENTIRE batch passed, push its whole directory in one call: `python -m uv run --extra deploy dotenv -f .env.local run -- python scripts/r2_push.py data/predictions/submitted/<label> submitted/<label>`.
- If a track had one or more shows fail verification (per STEP 3's per-show exclusion), do NOT push that track's directory wholesale — push only the passing files individually: `... r2_push.py data/predictions/submitted/<label>/<showdate>.json submitted/<label>/<showdate>.json` for each passing showdate. The failing show's local file stays in place (untouched, for a human to fix later) but must not reach R2.
- Never fall back to pushing the whole `data/predictions/submitted` directory in one shot when any show anywhere failed verification — that would sweep the rejected file(s) along with everything else.

If any push call fails (missing creds, no network, missing boto3), stop and report the failure clearly — do not consider the run done, and do NOT write the bookmark file (leave it as-is so the next run retries). This push is what makes the next scheduled GitHub Actions publish run fold your tracks in as `mcp:<label>` sources; you are not responsible for running the full `phishpred publish` pipeline yourself, that stays on its own separate schedule.

If the push succeeds, write the new max show_index (from STEP 1) to `tmp/prediction-orchestrator-state.txt`, overwriting whatever was there, even if one or two individual tracks/shows failed verification and got skipped above — a partial-but-real publish still counts as "the tracks are caught up," and any skipped track/show is a one-off for a human to backfill later, not a reason to re-run everything again tomorrow.

STEP 5 — report. Summarize: whether a new show triggered this run (and which showdate(s), and old vs. new max show_index / bookmark value), which of the three live tracks submitted successfully vs. failed verification and why, song/setlist counts per track, and the r2_push result. Keep it concise.
