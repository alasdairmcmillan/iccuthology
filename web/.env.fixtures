# Offline UI mode: `vite --mode fixtures` loads this instead of .env.local, so
# the bundled fixtures render without pointing at production. Use it to develop
# or verify states the live artifact cannot show yet -- e.g. an ELIMINATED track
# before the scoreboard carrying models[*].status has been published.
VITE_API_BASE=
VITE_USE_FIXTURES=true
