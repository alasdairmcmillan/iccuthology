{
  "name": "phish-predictor-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serve tier: static assets (../web/dist) + read-only /api/* JSON API over R2 snapshots (DEPLOY-CONTRACTS.md).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5",
    "wrangler": "^4.0.0"
  }
}
